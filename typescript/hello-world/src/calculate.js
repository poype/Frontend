/**
 * adding calculating
 * @param {number} num1 - first number
 * @param {number} num2 - second number
 * @returns {number} calculating result
 */
export function add(num1, num2) {
  return num1 + num2;
}