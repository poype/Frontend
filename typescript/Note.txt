tsc --init    // create a configuration file called tsconfig.json

tsc    // with tsconfig.json file in place, we can compile our code simply by running tsc without any arguments

debug
launch.json file is generated by vscode automatically.

you must compile code to generate map file before execute debug. 
if you change some code, you must recompile code to generate new map file so that you can debug new code.